// voy recorriendo el segundo string y si aparece 

// recorrer en orden str 1
// en str 2 voy recorriendo y si aparece el caracter de str 1 muevo todo y relleno con 0

// funcion mover y rellenar (trim by x character)
    // recibe un string y un char
    // recorre el string y si encuentra el char
    // llama a "machacar todo hacia la izquierda y rellena con 0" que recibe uns str y un index
    // el char puede aparecer más veces y debe hacer lo mismo con todas las ocurrencias

// main

// si arguv es 3 sigue
    // check que pasa si alguno de ambos es ""

    // i = 0
    // function check si este char de str1 está en algún lado de str2
        // si str2[i] == str1[i] si sique está
        // lo imprime y llamar a mover y rellenar aka trim sobre str2 mover_y_rellenar(str, char);
        // i++;

// si no es 3 imprimir barra n

